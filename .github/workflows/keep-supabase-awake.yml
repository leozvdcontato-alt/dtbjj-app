name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 12 */2 * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase database
        run: |
          curl --fail --show-error --silent \
            -X POST \
            "https://bpoxaxwczezfhncntomq.supabase.co/rest/v1/rpc/keepalive" \
            -H "apikey: sb_publishable_uFEHMHKZd6CIiZgrf1ip5A_0AF-FLYT" \
            -H "Content-Type: application/json" \
            -d '{}'
