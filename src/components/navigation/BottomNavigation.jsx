import { House, Users, GraduationCap, Ellipsis } from "lucide-react";

export default function BottomNavigation({ tela, setTela }) {
  return (
<nav className="fixed bottom-0 left-0 w-full h-20 bg-zinc-950 border-t border-zinc-800 flex items-center justify-around">
        Bottom Navigation
  </nav>
);
}